# =============================================================================
# Backend Environment Variables Template
# =============================================================================
# Copy this file to .env and update with your actual values
# DO NOT commit .env to version control (it's gitignored)

# =============================================================================
# REQUIRED VARIABLES (no defaults - must be set)
# =============================================================================

# Database Configuration
# PostgreSQL connection string (asyncpg driver)
DATABASE_URI=postgresql+asyncpg://openalpha_user:openalpha_password@localhost:5432/openalpha

# Security
# Generate a secure random string for JWT tokens and session security
# Use: openssl rand -hex 32
SECRET_KEY=your-secret-key-here-generate-a-secure-random-string

# Redis Configuration
# Redis connection string for caching and background jobs
REDIS_URL=redis://localhost:6379

# Superuser Account (created on first startup)
# Initial admin user credentials for the application
FIRST_SUPERUSER_EMAIL=admin@openalpha.com
FIRST_SUPERUSER_PASSWORD=changeme123

# Email Configuration
# Default email address for sending emails (must be valid email format)
DEFAULT_FROM_EMAIL=noreply@openalpha.com

# =============================================================================
# OPTIONAL VARIABLES (have defaults or are optional)
# =============================================================================

# Environment Mode
# Set to 'dev' or 'prod' (default: 'dev')
ENVIRONMENT=dev

# Debug Mode
# Enable debug logging and detailed error messages (default: false)
DEBUG=true

# Server Configuration
# Server host URL (default: http://localhost:8000)
SERVER_HOST=http://localhost:8000

# CORS Origins
# Comma-separated list of allowed frontend origins
# Format: http://localhost:5173,http://localhost:3000
BACKEND_CORS_ORIGINS=http://localhost:5173,http://localhost:3000

# Email Service (AWS SES) - Optional
# Only needed if EMAILS_ENABLED=true
# SES_ACCESS_KEY=
# SES_SECRET_KEY=
# SES_REGION=us-east-1

# Email Configuration - Optional
# Display name for sent emails (optional)
DEFAULT_FROM_NAME=OpenAlpha

# Email Sending
# Enable email sending (default: false)
# Requires: SMTP_HOST, SMTP_PORT, DEFAULT_FROM_EMAIL
EMAILS_ENABLED=false

# Error Tracking
# Sentry DSN for production error tracking (optional)
# SENTRY_DSN=

# =============================================================================
# NOTES
# =============================================================================
# - DATABASE_URI uses postgresql+asyncpg:// for async SQLAlchemy connections
# - SECRET_KEY should be a long, random string (32+ characters recommended)
# - FIRST_SUPERUSER credentials are only used on first startup
# - For production, set ENVIRONMENT=prod and DEBUG=false
# - BACKEND_CORS_ORIGINS must include your frontend URL(s) in production
